-- SnakeConfig - Configuration for AI Snakes ONLY
-- Player snakes use SnakeSystemIntegration's DEFAULT_CONFIG
-- This is a ModuleScript inside ReplicatedStorage

return {
	-- AI SNAKE CONFIGURATION
	AI = {
		-- Movement Settings
		BaseSpeed = 10,             -- Base movement speed
		BoostSpeed = 24,            -- Speed when boosting
		TurnSpeed = 1.8,            -- Turn rate (higher = sharper turns)
		FollowSpeed = 0.95,         -- How quickly segments follow head
		BoostFollowSpeed = 0.99,    -- Follow speed when boosting
		
		-- Size and Spacing
		HeadSize = Vector3.new(3, 3, 3),
		SegmentSize = Vector3.new(2.5, 2.5, 2.5),
		SegmentSpacing = 2.2,       -- Space between segments
		
		-- Visual Settings
		BeamWidthMultiplier = 0.9,  -- Beam width relative to segments
		GlowIntensity = 1.8,        -- Glow brightness
		GlowRange = 4,              -- Glow distance
		HeadMaterial = Enum.Material.ForceField,
		BodyMaterial = Enum.Material.Neon,
		
		-- Colors (Default AI appearance)
		HeadColor = Color3.fromRGB(76, 217, 100),
		BodyColors = {
			Color3.fromRGB(60, 180, 80),
			Color3.fromRGB(80, 200, 100),
			Color3.fromRGB(100, 220, 120),
			Color3.fromRGB(80, 200, 100),
			Color3.fromRGB(60, 180, 80),
		},
		
		-- Behavior Settings
		OrbSeekRange = 80,          -- Detection range for orbs
		ThreatDetectionRange = 60,  -- Detection range for threats
		BoostChance = 0.15,         -- Chance to boost per update
		
		-- Growth Settings
		InitialLength = 10,         -- Starting length for AI snakes
		MaxLength = 2000,           -- Maximum AI snake length
		OrbValue = 5,               -- Segments gained per orb
		
		-- Performance Settings
		UpdateRate = 1,             -- Update frequency
		LODDistance = 120,          -- Level of detail distance
	},
	
	-- LEGACY SETTINGS (kept for compatibility with old code)
	-- These are here in case other scripts reference them
	MaxSegments = 2450,
	SegmentGap = 3.0,
	MinDistance = 0.02,
	PathSmoothness = 0.9,
	BoostDrainRate = 3,
	
	-- Death orb return values (used by both AI and players)
	DeathOrbReturn = {
		small = 0.30,    -- Small snakes (< 100): return 30%
		medium = 0.25,   -- Medium snakes (100-200): return 25%
		large = 0.20,    -- Large snakes (200-500): return 20%
		huge = 0.15,     -- Huge snakes (500+): return 15%
		cap = 150        -- Max value from death orbs
	}
}
